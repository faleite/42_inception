# conf/default.conf
server {
    listen 80;
    server_name localhost;

    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}

# server {
#     listen 443 ssl;
#     server_name localhost;

#     root /var/www/html;
#     index index.html;

#     # Aqui você vai adicionar depois os certificados SSL
#     # ssl_certificate /etc/nginx/ssl/nginx.crt;
#     # ssl_certificate_key /etc/nginx/ssl/nginx.key;

#     location / {
#         try_files $uri $uri/ =404;
#     }
# }